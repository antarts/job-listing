<h1> <%= @job.title %> </h1>

<p>
  <%= markdown(@job.description) %>

</p>
<div class="text-center">
  <%= link_to("Send a Resume", new_job_resume_path(@job), :style => "font-size: 30px; text-decoration:underline;") %>
</div>
